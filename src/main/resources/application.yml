spring:
  application:
    name: moco-connect

server:
  port: 0

application:
  settings:
    moco:
      url: https://encircle360.mocoapp.com
      token: your-moco-api-token
    backup:
      path: "your-backup-path"
